
console.log "utils loading"

#widow.document.dir = (object) ->
#        methods = [];
#        for (z in object)
#            if (typeof(z) != 'number') {
#                methods.push(z);
#            }
#            return methods.join(', ');
#
